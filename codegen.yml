overwrite: true
schema: "api/schema/**/*.graphql"
documents: null
generates:
  gen/schema-type-def.d.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
